{
    "version": "0.2.0",
    "configurations": [
        {
            "name": "Run Zed Compiler",
            "type": "cppdbg",
            "request": "launch",
            "program": "${workspaceFolder}/compiler",
            "args": ["${file}"],
            "preLaunchTask": "build-compiler",
            "cwd": "${workspaceFolder}",
            "externalConsole": false,
            "MIMode": "gdb",
            "miDebuggerPath": "C:/msys64/mingw64/bin/gdb.exe"
        }
    ]
}
